# Vibe Watch (BlueOS)

vivo Watch 5 交互 App：展示 `action_prompt`，回传 `action_response` 到手机 Vibe Bridge。

## 状态

待使用 BlueOS Studio 创建工程。协议类型见 `crates/vibe-protocol`。

## 开发

- 语言：JavaScript（`.ux` + 穿戴业务 Kit）
- 工具：BlueOS Studio
- 需安装 vivo 运动健康（手机端互联底座）
